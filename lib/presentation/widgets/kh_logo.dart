import 'package:flutter/material.dart';

class KHLogo extends StatelessWidget {
  const KHLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'KabarHarian',
      style: TextStyle(fontSize: 24, letterSpacing: -2),
    );
  }
}
